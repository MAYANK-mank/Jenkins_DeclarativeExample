pipeline {
  agent any
  stages {
    stage('Build') {
      steps {echo "Build is successful"}
    }
    stage('Test') {
      steps {echo "Test is complete"}
    }
    stage('Deploy') {
      steps {echo "Deploy is successful"}
    }
    stage('Release') {
      steps {echo "Release is successful"}
    }  
  }
}
